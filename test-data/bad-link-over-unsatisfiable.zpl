
# An "over" clause naming a link attribute that no configured link carries.
# The compiler must reject this: the statement can never match anything.

define database as a service.

allow redhead users to access database over nosuchtag links.
